services:
  bsm1-simulator:
    build: .
    container_name: bsm1-api
    ports:
      - "8000:8000"
    volumes:
      - ./results:/app/results
    environment:
      - BSM1_T_DAYS=200
      - BSM1_TOLERANCE=0.05
    restart: unless-stopped
    mem_limit: 2g
